//Styles
import "../styles/Logout.css";

const Logout = () => {
  return (
    <div className="logout">
      <p>Are you sure you want to logout?</p>
      <p>Only log members can use the application</p>
    </div>
  );
};

export default Logout;
